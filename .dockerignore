# Node dependencies (reinstalled during build)
node_modules
frontend/node_modules
backend/node_modules

# Build artifacts (rebuilt during build)
frontend/.next
backend/dist

# Git history
.git

# Developer secrets — never bake into image
.env

# Generated AEM projects (trainees generate these at runtime)
aem-projects/

# SQLite chat history
*.db

# macOS artifacts
.DS_Store
